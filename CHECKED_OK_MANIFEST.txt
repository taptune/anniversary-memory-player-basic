Anniversary Memory Player Basic v14 - CHANGED FILES ONLY

Included files:
- index.html
- style.css
- script.js
- config.js
- CHECKED_OK_MANIFEST.txt

Not included:
- audio/
- images/

V11 fixes:
- Pause/resume no longer waits for the needle animation.
- When paused, tapping play starts the audio immediately and the needle swings back in at the same time.
- If the needle is still returning when you tap play, the return animation is interrupted and it swings back onto the record.
- The play/pause button beside the progress bar is still removed.
- cacheVersion updated to v11.

V12 notes:
- Added image transition lock so double-tapping too fast cannot make the cover fade jump.
- Sleeve now stays visible parked on the left after the first opening animation.
- Changing songs does not bring the sleeve back to the middle and does not hide it.
- cacheVersion updated to v12.

V13 notes:
- Changed-files-only package. No audio or images included.
- Song change animation added: sleeve appears on the left, old tape moves into it, new tape slides in from the side, then sleeve disappears.
- First start still uses the sleeve pull-out animation.
- Image spam lock from v12 is kept.
- cacheVersion updated to v13.

V14 notes:
- Fixed the new tape entry animation.
- During song change, the new tape now starts from the left sleeve position where the old tape disappeared.
- It no longer enters from the top-right side.
- cacheVersion updated to v14.

V32 final notes:
- Mobile first screen now fits without scrolling on common phone sizes.
- Romantic warm background treatment added.
- Opening animation adjusted so sleeve appears first, moves left, then tape pulls out.
- Needle is hidden at the start and appears only after the sleeve moves left.
- Tonearm is split into transparent base and arm images.
- Start button uses a two-column layout: play icon on the left, text on two rows.
- Top title is "Adam & Eve".
- Sleeve text is "MEMORY OF" / "A&E".
- cacheVersion updated to v32.
